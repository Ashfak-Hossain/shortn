apiVersion: v1
kind: Service
metadata:
  name: shortn-redis
  labels:
    app: shortn-redis
spec:
  type: ClusterIP # plain ClusterIP — not headless; it's stateless
  selector:
    app: shortn-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
