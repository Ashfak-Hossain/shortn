{{- if .Values.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shortn-web
  labels:
    app: shortn-web
spec:
  selector:
    app: shortn-web
  ports:
    - port: 80
      targetPort: 80
{{- end }}
